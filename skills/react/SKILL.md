---
name: react
version: 1.0.0
description: React application conventions
tags: [coding, react, frontend]
requiredTools: [filesystem.read, filesystem.write, terminal.run]
---
# React Skill

- Use function components + hooks; avoid class components in new code.
- Keep state as local as possible; lift only when necessary.
- Co-locate component, styles, and tests.
- Prefer composition over prop-drilling; consider context or a state library for deeply shared state.
