# Custom Skills

Drop your own skill directories here, each containing a `SKILL.md` with
YAML frontmatter metadata and an instructions body. See any of the sibling
skill directories (e.g. `../python/SKILL.md`) for the expected format.
